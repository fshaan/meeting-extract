# v0.3.0 Prompt 模板优化实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修正 v0.2.0 的质量退化并将 Claude 版升级为 v0.3.0 基准，确保 4 个模型版本结构和规则一致。

**Architecture:** Claude-first 级联——先完成 Claude 版两套模板的全部改动（项 1/2/3/5/7/8/9），再基于它级联更新 Gemini/GLM/Qwen（项 1/2/3/4/5/6）。纯 prompt 模板文本编辑，无代码或脚本变更。

**Tech Stack:** Markdown prompt 模板，Git 版本管理

---

## 文件清单

| 文件 | 操作 | 涉及修改项 |
|------|------|-----------|
| `Prompt/会议纪要提取_Claude版.md` | 修改 | 1, 2, 8 |
| `Prompt/会议发言提取_Claude版.md` | 修改 | 3, 5, 7, 9 |
| `Prompt/会议纪要提取_Gemini版.md` | 修改 | 1, 2 |
| `Prompt/会议纪要提取_GLM版.md` | 修改 | 1, 2 |
| `Prompt/会议纪要提取_Qwen版.md` | 修改 | 1, 2 |
| `Prompt/会议发言提取_Gemini版.md` | 修改 | 3, 4, 5, 6 |
| `Prompt/会议发言提取_GLM版.md` | 修改 | 3, 4, 5, 6 |
| `Prompt/会议发言提取_Qwen版.md` | 修改 | 3, 4, 5, 6 |
| `VERSION` | 修改 | 版本号 → 0.3.0 |
| `CHANGELOG.md` | 修改 | 追加 v0.3.0 条目 |

---

## Task 1: Claude 版会议纪要 — 项 8（去"模块"标签 + 词汇确认表）

**Files:**
- Modify: `Prompt/会议纪要提取_Claude版.md`

- [ ] **Step 1: 修改 `<task>` 中的模块引用**

```diff
- 严格按照 <output_format> 中定义的六大模块输出。不得遗漏任何模块，不得自行新增模块。
+ 严格按照 <output_format> 中定义的各章节输出。不得遗漏任何章节，不得自行新增章节。
```

- [ ] **Step 2: 去掉 `<output_format>` 中 6 个章节标题的"模块N："前缀**

逐一替换：

```diff
- ## 模块一：会议基本信息
+ ## 会议基本信息

- ## 模块二：会议摘要
+ ## 会议摘要

- ## 模块三：议题与决议
+ ## 议题与决议

- ## 模块四：待办事项（Action Items）
+ ## 待办事项（Action Items）

- ## 模块五：分歧与风险记录
+ ## 分歧与风险记录

- ## 模块六：关键发言摘录
+ ## 关键发言摘录
```

- [ ] **Step 3: 在关键发言摘录章节之后、`</output_format>` 之前新增词汇确认表章节**

在 `> **[发言人]**：「摘录内容」` 之后插入：

```markdown

## 词汇确认表

（汇总文中出现的关键人名、企业/组织名称、专业术语，供客户确认拼写和用法）

| 序号 | 词汇 | 类型 | 文中用法 | 备注 |
|------|------|------|---------|------|
| 1 | | 人名/组织/术语 | | |
```

- [ ] **Step 4: 在 `<instructions>` 核心规则中新增第 7 条**

在第 6 条「分歧如实记录」之后新增：

```markdown
7. **词汇确认**：关键人名、企业/组织名称、专业术语在文末「词汇确认表」中汇总整理，供客户确认拼写和用法。
```

- [ ] **Step 5: 验证文件结构完整性**

Run: `grep -n "^##" "Prompt/会议纪要提取_Claude版.md"`

Expected: 7 个章节标题（会议基本信息、会议摘要、议题与决议、待办事项、分歧与风险记录、关键发言摘录、词汇确认表），无"模块"字样。

---

## Task 2: Claude 版会议纪要 — 项 1（柔性书面化）+ 项 2（时间锚定内部化）

**Files:**
- Modify: `Prompt/会议纪要提取_Claude版.md`

- [ ] **Step 1: 修改核心规则第 3 条（项 1）**

```diff
- 3. **去噪整理**：自动修正明显的语音转写错误（如同音错字），去除口头禅、重复语句和填充词，但不得改变发言人的原始立场和表达意图。
+ 3. **去噪整理**：自动修正明显的语音转写错误（如同音错字），去除口头禅、重复语句和填充词，将口语化表达整理为书面用语，但保留发言人的原始用词习惯和关键表述，不得改变原始立场和表达意图。
```

- [ ] **Step 2: 修改质量约束（项 1 联动）**

```diff
- - 使用书面中文，避免口语化表达
+ - 使用书面中文，保留发言人原始用词和专业术语
```

- [ ] **Step 3: 修改核心规则第 4 条（项 2）**

```diff
- 4. **时间锚定**：当转写稿包含时间戳时，在关键议题和重要决议旁标注对应时间区间，方便回溯原始录音。
+ 4. **时间锚定**：当转写稿包含时间戳时，利用时间信息辅助定位发言归属和议题边界，但输出中不显示时间戳。
```

- [ ] **Step 4: 删除议题与决议中的时间区间提示（项 2 联动）**

```diff
- - **议题名称**（附时间区间，如有）
+ - **议题名称**
```

- [ ] **Step 5: 验证修改**

Run: `grep -n "完全转化\|避免口语\|时间区间" "Prompt/会议纪要提取_Claude版.md"`

Expected: 无匹配结果。

- [ ] **Step 6: 提交 Claude 会议纪要改动**

```bash
git add "Prompt/会议纪要提取_Claude版.md"
git commit -m "refactor(prompt): upgrade Claude meeting minutes template to v0.3.0

- Remove '模块N' labels from section headings
- Add glossary table (词汇确认表) section
- Soften verbatim-to-formal rewriting rule (item 1)
- Internalize timestamp anchoring as processing hint (item 2)"
```

---

## Task 3: Claude 版发言提取 — 项 7（改第三人称）

**Files:**
- Modify: `Prompt/会议发言提取_Claude版.md`

- [ ] **Step 1: 修改 `<context>` 末尾的视角指令**

```diff
- 你MUST以第一人称视角还原目标人员的观点体系，严禁使用"某某提出…""某某认为…"等第三人称转述话术。
+ 你MUST以第三人称客观视角陈述目标人员的观点体系，使用「某某指出…」「某某认为…」「某某强调…」等专业分析表述。严禁使用第一人称。
```

- [ ] **Step 2: 修改 `<output_format>` 核心观点模板中的视角描述**

```diff
- （以第一人称视角展开完整论述，融入互动中被接受的补充内容，每段末尾标注来源时间戳）
+ （以第三人称客观视角展开完整论述，融入互动中被接受的补充内容，每段末尾标注来源时间戳）
```

- [ ] **Step 3: 修改执行方案模板中的视角描述**

```diff
- （以第一人称还原完整方案描述）
+ （以第三人称客观视角陈述完整方案描述）
```

- [ ] **Step 4: 验证无第一人称残留**

Run: `grep -n "第一人称" "Prompt/会议发言提取_Claude版.md"`

Expected: 无匹配结果。

---

## Task 4: Claude 版发言提取 — 项 9（错别字策略）+ 项 5（用词规则）+ 新增章节

**Files:**
- Modify: `Prompt/会议发言提取_Claude版.md`

- [ ] **Step 1: 修改 `<constraints>` 第 2 条（项 9 错别字策略）**

```diff
- - 当转写文件中存在语音识别错误或语义不清的片段时，用 [原文存疑：原始文本] 标注，不要擅自修正语义
+ - 不符合逻辑的错别字（如语音转写导致的同音错字），根据上下文进行合理纠正，直接使用纠正后的文本；无法根据上下文确定正确含义的，在文末「需核实」章节中单独列出。
```

- [ ] **Step 2: 修改 `<constraints>` 第 3 条（项 5 用词规则）**

```diff
- - 将口语化表达转换为书面语，但MUST保留原始发言的核心用词和关键术语，不得替换为同义词
+ - 将口语化表达转换为专业书面用语，MUST保留原始发言的核心术语和关键表述，口语特征（语气词、重复、不完整句式）可做书面化专业化改写，但不得改变原意。
```

- [ ] **Step 3: 在 `<constraints>` 中新增词汇确认规则**

在最后一条（行动项区分提出人/执行人）之后新增：

```markdown
- 关键人名、企业/组织名称、专业术语MUST在文末「词汇确认表」中汇总整理，供客户确认拼写和用法。
```

- [ ] **Step 4: 修改观点演化轨迹章节（项 3）**

将 `<output_format>` 中的第五章替换为：

```diff
- ## 五、观点演化轨迹
- （记录目标人员在会议过程中观点发生变化的关键节点）
- - **HH:MM:SS** 初始观点：……
- - **HH:MM:SS** 触发变化：{某人} 提出……
- - **HH:MM:SS** 修正后观点：……
+ ## 五、观点演化轨迹
+ （记录目标人员在会议过程中观点发生显著变化的所有关键节点，按时间顺序逐一列出）
+
+ - **HH:MM:SS** [初始立场] ……
+ - **HH:MM:SS** [外部触发] {某人} 提出……
+ - **HH:MM:SS** [立场调整] ……
+ - **HH:MM:SS** [再次触发] ……
+ - **HH:MM:SS** [最终立场] ……
+ （按实际演化节点数量扩展，不限于上述示例数量）
```

- [ ] **Step 5: 在观点演化轨迹之后新增「需核实」章节**

在观点演化轨迹章节之后、文末分隔线之前插入：

```markdown

## 六、需核实
| 序号 | 原始转写文本 | 可能含义 | 上下文位置 |
|------|-------------|---------|-----------|
| 1 | | | |

> 若全文无需核实项，写"本次提取无需核实内容"
```

- [ ] **Step 6: 新增「词汇确认表」章节**

紧接「需核实」之后插入：

```markdown

## 七、词汇确认表
| 序号 | 词汇 | 类型 | 文中用法 | 备注 |
|------|------|------|---------|------|
| 1 | | 人名/组织/术语 | | |
```

- [ ] **Step 7: 更新文末声明**

```diff
- *本文档基于会议转写文件自动提炼，标注 [原文存疑] 的内容需人工复核。*
+ *本文档基于会议转写文件自动提炼，「需核实」和「词汇确认表」章节需客户审阅确认。*
```

- [ ] **Step 8: 验证修改**

Run: `grep -n "原文存疑\|第一人称\|不要擅自" "Prompt/会议发言提取_Claude版.md"`

Expected: 无匹配结果。

Run: `grep -c "^##" "Prompt/会议发言提取_Claude版.md"`

Expected: 输出包含「需核实」和「词汇确认表」章节。

- [ ] **Step 9: 提交 Claude 发言提取改动**

```bash
git add "Prompt/会议发言提取_Claude版.md"
git commit -m "refactor(prompt): upgrade Claude speaker extraction template to v0.3.0

- Switch from first-person to third-person perspective (item 7)
- Adopt smart typo correction + verification section (item 9)
- Soften wording rule to preserve key terms (item 5)
- Expand viewpoint evolution to multi-node timestamped format (item 3)
- Add glossary table and verification sections"
```

---

## Task 5: Gemini 版会议纪要 — 项 1 + 项 2

**Files:**
- Modify: `Prompt/会议纪要提取_Gemini版.md`

- [ ] **Step 1: 修改核心规则第 3 条（项 1 书面化）**

```diff
- 3. **去噪整理**：自动修正明显的语音转写错误（如同音错字），去除口头禅、重复语句和填充词，将口语化表达完全转化为专业书面用语，但不得改变发言人的原始立场和表达意图。
+ 3. **去噪整理**：自动修正明显的语音转写错误（如同音错字），去除口头禅、重复语句和填充词，将口语化表达整理为书面用语，但保留发言人的原始用词习惯和关键表述，不得改变原始立场和表达意图。
```

- [ ] **Step 2: 修改质量约束（项 1 联动）**

```diff
- - 使用书面中文，将全部口语化表达转化为专业书面用语
+ - 使用书面中文，保留发言人原始用词和专业术语
```

- [ ] **Step 3: 在核心规则第 5 条之后新增时间锚定规则（项 2）**

在「5. **分歧如实记录**」之后、「6. **词汇确认**」之前插入新的第 6 条，原第 6 条顺延为第 7 条：

```markdown
6. **时间锚定**：当转写稿包含时间戳时，利用时间信息辅助定位发言归属和议题边界，但输出中不显示时间戳。
```

- [ ] **Step 4: 验证**

Run: `grep -n "完全转化\|全部口语" "Prompt/会议纪要提取_Gemini版.md"`

Expected: 无匹配结果。

- [ ] **Step 5: 提交**

```bash
git add "Prompt/会议纪要提取_Gemini版.md"
git commit -m "refactor(prompt): update Gemini meeting minutes template for v0.3.0

- Soften verbatim-to-formal rewriting rule (item 1)
- Add internalized timestamp anchoring rule (item 2)"
```

---

## Task 6: GLM 版会议纪要 — 项 1 + 项 2

**Files:**
- Modify: `Prompt/会议纪要提取_GLM版.md`

- [ ] **Step 1: 修改 system 强制规则第 3 条（项 1 书面化）**

```diff
- - 修正明显转写错误，去除口头禅，将口语化表达完全转化为专业书面用语，但不改变原意
+ - 修正明显转写错误，去除口头禅，将口语化表达整理为书面用语，但保留发言人的原始用词习惯和关键表述，不改变原意
```

- [ ] **Step 2: 修改 system 末尾质量行（项 1 联动）**

```diff
- - 将全部口语化表达转化为专业书面用语
+ - 保留发言人原始用词和专业术语
```

- [ ] **Step 3: 在 system 强制规则中新增时间锚定规则（项 2）**

在「未达成一致的议题如实记录各方立场，不折中、不建议」之后新增：

```markdown
- 当转写稿包含时间戳时，利用时间信息辅助定位发言归属和议题边界，但输出中不显示时间戳
```

- [ ] **Step 4: 验证**

Run: `grep -n "完全转化\|全部口语" "Prompt/会议纪要提取_GLM版.md"`

Expected: 无匹配结果。

- [ ] **Step 5: 提交**

```bash
git add "Prompt/会议纪要提取_GLM版.md"
git commit -m "refactor(prompt): update GLM meeting minutes template for v0.3.0

- Soften verbatim-to-formal rewriting rule (item 1)
- Add internalized timestamp anchoring rule (item 2)"
```

---

## Task 7: Qwen 版会议纪要 — 项 1 + 项 2

**Files:**
- Modify: `Prompt/会议纪要提取_Qwen版.md`

- [ ] **Step 1: 修改 system 强制规则第 3 条（项 1 书面化）**

```diff
- 3. 自动修正明显的语音转写错误（同音错字），去除口头禅和填充词，将口语化表达完全转化为专业书面用语，但不改变发言人原始立场
+ 3. 自动修正明显的语音转写错误（同音错字），去除口头禅和填充词，将口语化表达整理为书面用语，但保留发言人的原始用词习惯和关键表述，不改变发言人原始立场
```

- [ ] **Step 2: 修改 system 输出质量（项 1 联动）**

```diff
- - 将全部口语化表达转化为专业书面用语，保留原文专业术语和英文专有名词
+ - 使用书面中文，保留发言人原始用词、专业术语和英文专有名词
```

- [ ] **Step 3: 在 system 强制规则中新增时间锚定规则（项 2）**

在第 5 条「未达成一致的议题如实记录各方立场，不折中、不判断、不建议」之后插入新的第 6 条，原第 6/7 条顺延为第 7/8 条：

```markdown
6. 当转写稿包含时间戳时，利用时间信息辅助定位发言归属和议题边界，但输出中不显示时间戳
```

- [ ] **Step 4: 验证**

Run: `grep -n "完全转化\|全部口语" "Prompt/会议纪要提取_Qwen版.md"`

Expected: 无匹配结果。

- [ ] **Step 5: 提交**

```bash
git add "Prompt/会议纪要提取_Qwen版.md"
git commit -m "refactor(prompt): update Qwen meeting minutes template for v0.3.0

- Soften verbatim-to-formal rewriting rule (item 1)
- Add internalized timestamp anchoring rule (item 2)"
```

---

## Task 8: Gemini 版发言提取 — 项 3 + 项 4 + 项 5 + 项 6

**Files:**
- Modify: `Prompt/会议发言提取_Gemini版.md`

- [ ] **Step 1: 修改 Hard Constraints 第 1 条（项 6 缓和极端词）**

```diff
- 1. 仅基于下方提供的转写文件内容进行提取和重构。不得推测、添加或编造转写文件中不存在的任何信息。如果某个信息不确定，标注 [uncertain]。
+ 1. 仅基于下方提供的转写文件内容进行提取和重构。不得推测、添加或编造转写文件中不存在的信息。如果某个信息不确定，标注 [uncertain]。
```

- [ ] **Step 2: 修改 Hard Constraints 第 3 条（项 5 用词规则）**

```diff
- 3. 将全部口语化表达完全转化为专业的书面用语，不必保留原始用词和语气特征。
+ 3. 将口语化表达转换为专业书面用语，MUST保留原始发言的核心术语和关键表述，口语特征（语气词、重复、不完整句式）可做书面化专业化改写，但不得改变原意。
```

- [ ] **Step 3: 修改 Hard Constraints 第 7 条（项 6 缓和极端词）**

```diff
- 7. 不要输出与下方输出模板无关的任何解释、总结、前言、后记或评论。直接按模板输出。
+ 7. 不要输出与下方输出模板无关的解释、总结或前言。直接按模板输出。
```

- [ ] **Step 4: 在 Hard Constraints 中新增时间戳规则（项 4）**

在第 5 条（行动项区分提出人/执行人）之后新增：

```markdown
6. 每个核心观点MUST标注来源时间戳，格式为 (来源: HH:MM:SS)
```

原第 6/7 条顺延为第 7/8 条。

- [ ] **Step 5: 修改核心观点模板（项 4 时间戳）**

```diff
- （以第三人称客观视角陈述完整论述，融入互动中被接受的补充内容）
+ （以第三人称客观视角陈述完整论述，融入互动中被接受的补充内容，每段末尾标注来源时间戳）
```

- [ ] **Step 6: 修改互动补充格式（项 4 时间戳）**

```diff
- > 互动补充：{补充人姓名} 提出了……，该补充被目标人员采纳并纳入上述论述。
+ > 互动补充：{补充人姓名} 提出了……，该补充被目标人员采纳并纳入上述论述。(来源: HH:MM:SS)
```

- [ ] **Step 7: 恢复实施步骤表头的来源时间戳列（项 4）**

```diff
- | 序号 | 步骤 | 负责人 | 时间节点 |
+ | 序号 | 步骤 | 负责人 | 时间节点 | 来源时间戳 |
```

- [ ] **Step 8: 替换观点演化轨迹章节（项 3）**

```diff
- ## 五、观点演化轨迹
- - 初始观点：……
- - 触发变化：{某人} 提出……
- - 修正后观点：……
+ ## 五、观点演化轨迹
+ （记录目标人员在会议过程中观点发生显著变化的所有关键节点，按时间顺序逐一列出）
+
+ - **HH:MM:SS** [初始立场] ……
+ - **HH:MM:SS** [外部触发] {某人} 提出……
+ - **HH:MM:SS** [立场调整] ……
+ - **HH:MM:SS** [再次触发] ……
+ - **HH:MM:SS** [最终立场] ……
+ （按实际演化节点数量扩展，不限于上述示例数量）
```

- [ ] **Step 9: 验证**

Run: `grep -n "完全转化\|不必保留\|任何信息\|任何解释" "Prompt/会议发言提取_Gemini版.md"`

Expected: 无匹配结果。

- [ ] **Step 10: 提交**

```bash
git add "Prompt/会议发言提取_Gemini版.md"
git commit -m "refactor(prompt): update Gemini speaker extraction template for v0.3.0

- Restore multi-node timestamped viewpoint evolution (item 3)
- Restore source timestamps to observations/interactions/steps (item 4)
- Soften wording rule to preserve key terms (item 5)
- Tone down absolute constraint wording (item 6)"
```

---

## Task 9: GLM 版发言提取 — 项 3 + 项 4 + 项 5 + 项 6

**Files:**
- Modify: `Prompt/会议发言提取_GLM版.md`

- [ ] **Step 1: 修改强制规则第 3 条（项 5 用词规则）**

```diff
- 3. 将全部口语化表达完全转化为专业的书面用语，不必保留原始用词和语气特征
+ 3. 将口语化表达转换为专业书面用语，MUST保留原始发言的核心术语和关键表述，口语特征（语气词、重复、不完整句式）可做书面化专业化改写，但不得改变原意。
```

- [ ] **Step 2: 修改强制规则第 7 条（项 6 缓和极端词）**

```diff
- 7. 输出必须完全匹配下方模板，不要添加模板之外的任何文字
+ 7. 输出必须完全匹配下方模板，不要添加模板之外的额外文字
```

- [ ] **Step 3: 在强制规则中新增时间戳规则（项 4）**

在第 5 条（行动项区分提出人/执行人）之后新增：

```markdown
6. 每个核心观点MUST标注来源时间戳，格式为 (来源: HH:MM:SS)
```

原第 6/7 条顺延为第 7/8 条。

- [ ] **Step 4: 修改核心观点模板（项 4 时间戳）**

```diff
- （第三人称客观视角完整论述，融入被采纳的互动补充。）
+ （第三人称客观视角完整论述，融入被采纳的互动补充，每段末尾标注来源时间戳。）
```

- [ ] **Step 5: 修改互动补充格式（项 4 时间戳）**

```diff
- > 互动补充：{补充人姓名} 提出了……，该补充被采纳。
+ > 互动补充：{补充人姓名} 提出了……，该补充被采纳。(来源: HH:MM:SS)
```

- [ ] **Step 6: 恢复实施步骤表头的来源时间戳列（项 4）**

```diff
- | 序号 | 步骤 | 负责人 | 时间节点 |
+ | 序号 | 步骤 | 负责人 | 时间节点 | 来源时间戳 |
```

- [ ] **Step 7: 替换观点演化轨迹章节（项 3）**

```diff
- ## 五、观点演化轨迹
- - 初始观点：……
- - 触发变化：{某人} 提出……
- - 修正后观点：……
+ ## 五、观点演化轨迹
+ （记录目标人员在会议过程中观点发生显著变化的所有关键节点，按时间顺序逐一列出）
+
+ - **HH:MM:SS** [初始立场] ……
+ - **HH:MM:SS** [外部触发] {某人} 提出……
+ - **HH:MM:SS** [立场调整] ……
+ - **HH:MM:SS** [再次触发] ……
+ - **HH:MM:SS** [最终立场] ……
+ （按实际演化节点数量扩展，不限于上述示例数量）
```

- [ ] **Step 8: 验证**

Run: `grep -n "完全转化\|不必保留\|任何文字" "Prompt/会议发言提取_GLM版.md"`

Expected: 无匹配结果。

- [ ] **Step 9: 提交**

```bash
git add "Prompt/会议发言提取_GLM版.md"
git commit -m "refactor(prompt): update GLM speaker extraction template for v0.3.0

- Restore multi-node timestamped viewpoint evolution (item 3)
- Restore source timestamps to observations/interactions/steps (item 4)
- Soften wording rule to preserve key terms (item 5)
- Tone down absolute constraint wording (item 6)"
```

---

## Task 10: Qwen 版发言提取 — 项 3 + 项 4 + 项 5 + 项 6

**Files:**
- Modify: `Prompt/会议发言提取_Qwen版.md`

- [ ] **Step 1: 修改强制规则第 3 条（项 5 用词规则）**

```diff
- 3. 将全部口语化表达完全转化为专业的书面用语，不必保留原始用词和语气特征
+ 3. 将口语化表达转换为专业书面用语，MUST保留原始发言的核心术语和关键表述，口语特征（语气词、重复、不完整句式）可做书面化专业化改写，但不得改变原意。
```

- [ ] **Step 2: 修改强制规则第 7 条（项 6 缓和极端词）**

```diff
- 7. 严格按照下方模板输出，不要添加任何模板之外的前言、后记或解释
+ 7. 严格按照下方模板输出，不要添加模板之外的前言、后记或解释
```

- [ ] **Step 3: 在强制规则中新增时间戳规则（项 4）**

在第 5 条（行动项区分提出人/执行人）之后新增：

```markdown
6. 每个核心观点MUST标注来源时间戳，格式为 (来源: HH:MM:SS)
```

原第 6/7 条顺延为第 7/8 条。

- [ ] **Step 4: 修改核心观点模板（项 4 时间戳）**

```diff
- （第三人称客观视角完整论述，融入被采纳的互动补充）
+ （第三人称客观视角完整论述，融入被采纳的互动补充，每段末尾标注来源时间戳）
```

- [ ] **Step 5: 修改互动补充格式（项 4 时间戳）**

```diff
- > 互动补充：{补充人姓名} 提出了……，该补充被采纳。
+ > 互动补充：{补充人姓名} 提出了……，该补充被采纳。(来源: HH:MM:SS)
```

- [ ] **Step 6: 恢复实施步骤表头的来源时间戳列（项 4）**

```diff
- | 序号 | 步骤 | 负责人 | 时间节点 |
+ | 序号 | 步骤 | 负责人 | 时间节点 | 来源时间戳 |
```

- [ ] **Step 7: 替换观点演化轨迹章节（项 3）**

```diff
- ## 五、观点演化轨迹
- - 初始观点：……
- - 触发变化：……
- - 修正后观点：……
+ ## 五、观点演化轨迹
+ （记录目标人员在会议过程中观点发生显著变化的所有关键节点，按时间顺序逐一列出）
+
+ - **HH:MM:SS** [初始立场] ……
+ - **HH:MM:SS** [外部触发] {某人} 提出……
+ - **HH:MM:SS** [立场调整] ……
+ - **HH:MM:SS** [再次触发] ……
+ - **HH:MM:SS** [最终立场] ……
+ （按实际演化节点数量扩展，不限于上述示例数量）
```

- [ ] **Step 8: 验证**

Run: `grep -n "完全转化\|不必保留\|添加任何" "Prompt/会议发言提取_Qwen版.md"`

Expected: 无匹配结果。

- [ ] **Step 9: 提交**

```bash
git add "Prompt/会议发言提取_Qwen版.md"
git commit -m "refactor(prompt): update Qwen speaker extraction template for v0.3.0

- Restore multi-node timestamped viewpoint evolution (item 3)
- Restore source timestamps to observations/interactions/steps (item 4)
- Soften wording rule to preserve key terms (item 5)
- Tone down absolute constraint wording (item 6)"
```

---

## Task 11: 版本号和 CHANGELOG

**Files:**
- Modify: `VERSION`
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 更新 VERSION 文件**

```diff
- 0.2.0
+ 0.3.0
```

- [ ] **Step 2: 在 CHANGELOG.md 顶部追加 v0.3.0 条目**

在现有内容最前面（标题之后）插入：

```markdown
## [0.3.0] - 2026-04-08

### Changed
- 会议纪要模板：回退"完全转化"为柔性书面化要求，保留发言人原始用词习惯（项 1）
- 会议纪要模板：时间锚定改为内部处理指令，输出不再显示时间戳（项 2）
- 发言提取模板：观点演化轨迹恢复多节点带时间戳格式（项 3）
- 发言提取模板：恢复来源时间戳到核心观点、互动补充、实施步骤（项 4）
- 发言提取模板：用词规则改为保留核心术语，口语特征可书面化改写（项 5）
- 发言提取 Gemini/GLM/Qwen：模板约束缓和，去掉"任何"极端词（项 6）
- Claude 版发言提取：改为第三人称客观视角（项 7）
- Claude 版会议纪要：去掉"模块N"标签，新增词汇确认表章节（项 8）
- Claude 版发言提取：统一错别字处理为"智能纠正 + 需核实"，新增需核实和词汇确认表章节（项 9）

### Notes
- 全部 8 个 prompt 模板文件（4 模型 × 2 模板类型）已同步更新
- Claude 版升级为 v0.3.0 基准模板，其他版本基于它级联适配
```

- [ ] **Step 3: 提交**

```bash
git add VERSION CHANGELOG.md
git commit -m "chore: bump version to 0.3.0 with changelog"
```

---

## 最终验证

- [ ] **Cross-check: 全部 8 个模板文件中不应再包含 v0.2.0 退化措辞**

```bash
grep -rn "完全转化\|全部口语\|不必保留" Prompt/
```

Expected: 无匹配结果。

- [ ] **Cross-check: 全部 4 个发言提取模板都包含多节点观点演化格式**

```bash
grep -l "初始立场.*外部触发.*立场调整" Prompt/会议发言提取_*.md
```

Expected: 4 个文件全部匹配。
